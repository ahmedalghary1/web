SECRET_KEY=replace-with-a-long-random-secret
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=
JWT_ACCESS_MINUTES=30
JWT_REFRESH_DAYS=7
LOGIN_THROTTLE=10/minute
# PostgreSQL later: set DB_ENGINE=django.db.backends.postgresql and DB_NAME,
# then extend config/settings.py with DB_USER, DB_PASSWORD, DB_HOST and DB_PORT.
